// Enum to represent various local storage keys used for application state
export enum Key {
    LOGGEDIN = '[KEY] LOGGEDIN',   // Key for storing the logged-in state of the user
    ROLE = '[KEY] ROLE'           // Key for storing the role of the user
}
